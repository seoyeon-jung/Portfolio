import styled from "styled-components";

export const Skills = () => {
  return (
    <Section>
      <Container>
        <Header>this is skills page</Header>
      </Container>
    </Section>
  );
};

const Section = styled.div`
  background-color: #fcecba;
  width: 100%;
  height: 100%;
`;

const Container = styled.div``;

const Header = styled.div`
  font-size: 40px;
  text-align: center;
`;
